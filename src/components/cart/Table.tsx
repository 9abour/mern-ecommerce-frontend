import React from "react";
import TableItem from "./TableItem";

const Table = () => {
	return (
		<table className="w-full">
			<TableItem />
			<TableItem />
		</table>
	);
};

export default Table;
